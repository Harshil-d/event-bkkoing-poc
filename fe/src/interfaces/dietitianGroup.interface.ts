export interface IDietitianGroupProfile {
  name: string;
  code: string;
  tagLine?: string;
  email: string;
  contactNumber: string;
  whatsAppNumber?: string;
}

export interface IDietitianGroupLink {
  linkType: string;
  link: string;
}
